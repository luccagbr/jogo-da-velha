let pessoa = {
    nome: "",
    idade: 0,
    cpf: 0
}

const informacoes = (pessoa) => {
    pessoa.nome = prompt("Digite seu nome:");
};

const nome_usuarios = (nome, idade, cpf) => {
    let token_validation = Math.random();

    return token_validation
}


informacoes(pessoa)
console.log(`Seu nome é ${pessoa.nome}`);
